/* class User(val id: Int, var name: String, var age: Int)

fun main(){
    val user = User(1, "Sean", 30)

    val name = user.name

    user.age = 41

    println("name: $name, ${user.age}")
    // 접근 메서드가 자동 내장 되어있기 때문에 신경쓸 필요 없음.
}

*/